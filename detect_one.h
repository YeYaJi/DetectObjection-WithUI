#ifndef DETECT_ONE_H
#define DETECT_ONE_H

#include <QObject>
#include <QWidget>

class detect_one : public QWidget
{
    Q_OBJECT
public:
    explicit detect_one(QWidget *parent = nullptr);

signals:

};

#endif // DETECT_ONE_H
